# Phase 16 — our engine vs vLLM

Generated by `profiles/phase16/engine_vs_vllm.py` from `workload.json`, `engine_freeze.json`, `results_ours.json` and `results_vllm.json` (if present).

Workload: greedy, fp32, `max_new_tokens=64`, EOS id 3. **single** = 1 request with a 128-token prompt; **batch** = 16 requests (prompts 32–125 tokens) arriving together, max 16 running at once. GPU: NVIDIA GeForce RTX 3050 6GB Laptop GPU.

## single

| engine | TTFT ms (mean) | ITL ms (mean) | ITL ms (p50) | tokens/s | total s | peak MB |
|---|---|---|---|---|---|---|
| ours: baseline | 5.1 | 3.18 | 3.13 | 311 | 0.206 | 141 |
| ours: Phase 15 optimized | 4.3 | 1.88 | 1.85 | 522 | 0.123 | 136 |

## batch

| engine | TTFT ms (mean) | ITL ms (mean) | ITL ms (p50) | tokens/s | total s | peak MB |
|---|---|---|---|---|---|---|
| ours: baseline | 54.4 | 17.74 | 17.72 | 867 | 1.172 | 296 |
| ours: Phase 15 optimized | 43.9 | 17.25 | 17.26 | 898 | 1.131 | 296 |

**vLLM side not run yet.** vLLM cannot be installed into this project's environment (it pins its own PyTorch/CUDA build; this env has PyTorch 2.13.0+cu130 on Python 3.14.6). Run `python profiles/phase16/engine_vs_vllm.py --engine vllm` in a separate vLLM environment, then `--report`. The model is already exported as a standard `LlamaForCausalLM` (`models/v1-llama`, verified against our model by `export_llama.py`), so vLLM runs it with no custom code.

## Which mechanisms explain the difference

| mechanism | our engine | vLLM | effect on this workload |
|---|---|---|---|
| kernel launches per decode step | ~424 eager (Phase 13); ~130 with torch.compile (Phase 15) | torch.compile'd model + CUDA graph replay: a decode step is ~1 graph launch | the dominant factor at this model size: decode is launch-bound (Phase 14/15) |
| batch layout | prefill right-padded to [B,T_max] + separate [B,1] decode forward | one flattened token batch per step (no padding), prefill + decode tokens mixed | no wasted FLOPs on padding; one forward per step instead of two |
| KV read for attention | paged: gather blocks back into contiguous K/V every step; batched decode: pad + torch.cat all rows every step | paged-attention kernel reads blocks in place via block_table; new K/V written by slot_mapping | removes O(B·L) copies per step; grows with context and batch size |
| attention kernel | SDPA memory-efficient fp32 kernel, repeat_interleave for GQA, masks | FlashAttention / FlashInfer / Triton varlen paged kernels with native GQA | fewer launches and bytes; fp32 limits which backends apply |
| LM head | all positions (last-only is a Phase 15 opt-in) | only logits_indices (last scheduled token) | prefill LM head is ~26% of prefill FLOPs (Phase 14) |
| sampling | Python loop, one Sampler call per request | one batched GPU sampler for all requests | per-request Python overhead scales with batch size |
| scheduling | per step: admit (slots + KV), prefill newcomers, decode actives; no token budget | token budget (max_num_batched_tokens), chunked prefill, preemption, prefix-cache lookup | better TTFT/ITL balance under load; neutral for a 16-request offline batch |
| process structure | one Python process, synchronous | EngineCore busy loop (optionally its own process), async output processing / detokenization | CPU work overlaps GPU work |

See `Docs/FINAL_ARCHITECTURE.md` §18 for the component-by-component mapping with vLLM source references.

